I'll verify the panel's load-bearing claims against the repo. First, let me locate and read the key files.VERIFIED: The two-pass scoring structure is exactly as the panel describes. In projects/wordsmith/src/scoring.js, score() runs a first loop over relics applying r.addSteel and r.addMult (lines 37–41), then a second loop applying r.xMult (lines 42–45), and returns Math.round(steel * mult) at line 47; the header comment at lines 3–6 states the intent, "multiplicative relics (xMult) apply last, so compounding stays with the rarest effects." The critic's point that each relic's hook executes twice is confirmed: relic.hook(ctx) is called at line 38 and again at line 43, once per loop, so a hypothetical relic returning both an additive field and xMult would contribute in both passes, and the "faithful mirror of execution" framing was correctly softened to mirroring effect order. The synthesizer's record correction also holds: RELICS is defined in scoring.js at lines 8–29, and no relics.js exists — the src directory contains only dictionary.js, letters.js, and scoring.js. The three shipped relics each return exactly one field, vowelMult returning addMult (line 15), bookend returning xMult (line 22), longhand returning addSteel (line 27), so none double-fires today and "pulse per contribution" is correctly labeled forward-looking hygiene.

The decisive round-2 dispute goes to the critic. lengthBonus in projects/wordsmith/src/letters.js lines 15–21 is a per-letter accumulator, "for (let i = 4; i < word.length; i++) bonus += i >= 7 ? LEN_TIER2 : LEN_TIER1" with LEN_TIER1 = 5 and LEN_TIER2 = 10, so a 6-letter word earns +10, a 7-letter word +15, and a 9-letter word 15 + 20 = 35, exactly the critic's arithmetic. The proposer's "three visually distinct ticks" with flat awards at letters 5 and 8 misstates this curve; the test at scoring.test.js line 20 independently confirms the per-letter shape, with 'planets' earning bonus 5+5+5. The comment at letters.js lines 9–11 does say "This is the primary skill axis, so the tiers live here as the single tuning point," so the panel's emphasis on teaching this curve through the animation is grounded in the code's own words. On validation, dictionary.js line 29 is precisely "return w.length >= MIN_LEN && dict.has(w)" with MIN_LEN = 2 declared at line 3 alongside the comment that slang is excluded at list-build time; a Set membership check is O(1) per tap and genuinely cannot prefix-hint, so live submit gating is the achievable fix as claimed. On the web page ruling's factual basis, package.json contains only "test": "node test/scoring.test.js" with no dependencies and no Expo scaffold, so there is nothing to link to yet. Running npm test yields 6 passed, and the 'level' fixture confirms xMult applies over additive mult ((1+2)*2 = 6), matching the claimed engine behavior end to end. Every load-bearing claim in the final synthesis checks out against the repo; the only panel errors were the critic's phantom relics.js citation (corrected in-round) and the proposer's tier-tick misreading (corrected in-round).

VERDICT: I endorse the round-2 synthesis with the critic's lengthBonus correction. Anchor the tile rack and submit control in the bottom thumb zone with the current-word strip directly above it, letters doubling as tap-to-remove targets; put the encounter target and running score as a glanceable top readout; keep the relic tray expanded by default through the first encounter, collapsing only after the player has seen a badge fire. Ship tap-to-select only — dragging fights one-handed reach and mis-drops, and the first on-device stage should carry one input model. The score-resolution animation plays at the word strip, not the top: a single continuous per-letter steel run where each letter from the 5th visibly adds its tile value plus a +5 length increment that steps to +10 at the 8th letter (one escalating run with a step, never discrete tier awards, because that is what lengthBonus actually computes and the code calls it the primary skill axis), then additive relic badges ticking steel and mult, then a visually distinct scale/pulse beat for xMult last, then the total flying up from the word strip to the encounter target to teach that relationship; badges should pulse per contribution rather than per relic since hooks run in both scoring passes. Distinguish steel from mult by shape and position, never hue alone, so the design survives light and dark modes and color-vision deficits. Do not build the companion web page now — package.json shows no Expo scaffold, no backend, and nothing to link; revisit only at store launch, and then it need show just the resolution animation and a store link. The one first-minute UX risk is the rejected submit: a new player who taps tiles into a non-word gets bounced before any scoring animation ever plays, so nothing about steel-times-mult is taught; kill it by revalidating isValid (a cheap Set lookup) on every tap and gating the submit button live, accepting that prefix hinting is impossible with a Set.